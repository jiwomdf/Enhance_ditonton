import 'package:flutter_test/flutter_test.dart';

void main() {
  setUp(() {});

  test('should get list of tv series from the repository', () async {
    // arrange
    //when(mockTvRepository.getTvList()).thenAnswer((_) async => Right());
    // act
    //final result = await usecase.execute();
    // assert
    //expect(result, Right());
  });
}
